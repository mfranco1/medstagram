[tool.black]
line-length = 100
target-version = ['py311']

[tool.ruff]
line-length = 100
target-version = "py311"
extend-select = ["I", "B", "E", "F", "W", "Q", "UP", "SIM"]
exclude = ["migrations", ".venv"]
fix = true
